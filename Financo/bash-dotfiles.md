***

es logear 

aws sso login

Descargar contenedor

```bash
./downloads3.sh
```

editar el comando insertDump.sh
poner la url donde se descargo la el archivo .sql que se se llama dump_bd_finanssoreal_121111111.sql
si se modifica la base de datos, se vuelve a descargar el archivo .sql y ejecutar el dowloads3.sh 

```bash
./insertDump.sh 
```

ejecutarlo 

```bash
./insertDump.sh 
```

